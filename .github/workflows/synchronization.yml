name: Fork
on:
  workflow_dispatch:

#  schedule:
#    - cron: 0 8 */5 * *

jobs: 
  build:
    runs-on: Ubuntu-20.04
    name: fork仓库
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    
    - name: fork
      uses: danshui-git/github-forks-sync-action@main
      with:
        github_token: ${{ secrets.REPO_TOKEN }}
        upstream_repository: CCnut/feed-netkeeper
        upstream_branch: LUCI-JS-UCITRACK
        target_repository: 281677160/feed-netkeeper
        target_branch: LUCI-JS-UCITRACK
        force: true
        tags: false
